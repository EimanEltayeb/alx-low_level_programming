#include "main.h"
/**
 * * main - maint function
 * @argc: num of arguments
 * @argv: the arguments
 * Return: 0
*/
int main(int argc, char **argv)
{
	if (argv[1] == NULL || argc != 2)
	{
		exit(98);
	}
	return (0);
}
