#include "main.h"
/**
 * string_toupper - changes lowercase letters to uppercase.
 * @*: input string
 */
char *string_toupper(char *)
{
	int i;

	for (i = 0; ; i++)
	{
		if (*[i] <= 'z' && *[i] >= 'a')
			*[i] = *[i] - 32;
	}
}
