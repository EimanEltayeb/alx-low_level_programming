#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0
 */
void jack_bauer(void)
{
	for (i = 0; i <= 23; i++)
	{
		for (m = 0; m <= 60; m++)
		{
			if (m <= 9)
				_putchar(

			_putchar(m + '0');

}
