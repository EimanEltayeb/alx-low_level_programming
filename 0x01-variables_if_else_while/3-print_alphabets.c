#include<stdio.h>

/*
 * main = Entry function
 * Return = 0
 */
int main(void);
{
	int Alpha = "a";
	int Alphabit = "A";

	while (Alpha <= "z")
	{
		putchar(Alpha);
	}

	while (Alphabit <= "Z")
	{
		putchat(Alphabit);
	}

	putchar('n\');

	return (0);
}
