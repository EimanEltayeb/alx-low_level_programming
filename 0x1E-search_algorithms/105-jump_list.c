#include "search_algos.h"
/**
 * jump_list - jump list searching
 * @list: the list
 * @size: size of list
 * @value: value to be searched for
 * Return: pointer to the node
 */
listint_t *jump_list(listint_t *list, size_t size, int value)
{
	if (!list)
		return (-1);
}
